{
  "name": "uno-game",
  "version": "1.0.0",
  "description": "Professional multiplayer UNO game with lobbies, house rules, bots, and spectators",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "start": "concurrently -k -n server,client -c blue,green \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start:prod": "npm run start:prod --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
